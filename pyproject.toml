[project]
name = "spikegame"
version = "0.1.0"
description = "a game"
readme = "readme.md"
requires-python = ">=3.13"
dependencies = [
    "pygame-ce~=2.5.6",
]

[build-system]
requires = ["uv_build>=0.9.13,<0.12.0"]
build-backend = "uv_build"
